interface:
  display_name: "P3C Security Rules"
  short_description: "Security rules for auth, masking, injection prevention, and validation."
  default_prompt: "Use $p3c-security-rules to review this API for security risks."
policy:
  allow_implicit_invocation: true
